version: 2
jobs:
  build:
    docker:
      - image: engrsdomingues/neopragma:v2
    steps:
      - checkout
      - run:
          name: Move, Build & test
          command: |
            mv src /neopragma/src
            ls -la /neopragma/src
            ls -la /neopragma/src/main
            ls -la /neopragma/src/main/cobol
            cd /neopragma
            ./run-examples