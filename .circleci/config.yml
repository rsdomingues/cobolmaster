version: 2
jobs:
  build:
    docker:
      - image: engrsdomingues/neopragma:v2
    steps:
      - checkout
      - run:
          name: Move app do build dir
          command: |
            mv src /neopragma/src
            cd /neopragma
      - run:
          name: Testing Sample
          command: ./run-examples