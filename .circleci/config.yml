version: 2
jobs:
  build:
    docker:
      - image: engrsdomingues/neopragma:v2
    steps:
      - checkout
      - run:
          name: Build Sample
          command: ls -la .
      - run:
          name: Testing Sample
          command: ls -la /neopragma/src